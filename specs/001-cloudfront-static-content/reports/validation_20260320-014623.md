## Validation Report: 001-cloudfront-static-content

Updated against the current branch state. Compared with the prior report, the implementation now:

- pins `hashicorp/aws` with `version = "~> 6.0"` in `/workspace/versions.tf:5-8`
- enforces stronger domain-to-hosted-zone relationship validation in `/workspace/variables.tf:66-72` and `/workspace/variables.tf:140-147`
- shortens S3 bucket names with hashed suffixes in `/workspace/locals.tf:5-8` to avoid prior bucket-length risk

### Design Conformance
- Modules: 8/8 from inventory present (mismatches: none)
- Wiring: 10/10 connections verified
- Variables: 10/10 declared with explicit types, descriptions, defaults, and validation blocks; domain/SAN-to-hosted-zone validation is now stronger than the original design text
- Provider config: default_tags present on both AWS providers; required `ManagedBy`, `Environment`, `Project`, and `Owner` tags are set in `/workspace/providers.tf:4-12` and `/workspace/providers.tf:21-29`; no static credentials found
- Raw resources: 0 (glue only: Yes)

Structured checklist:

- [x] All inventory modules are present:
  - `content_bucket` — `/workspace/main.tf:6`
  - `access_logs_bucket` — `/workspace/main.tf:35`
  - `acm_certificate` — `/workspace/main.tf:61`
  - `cloudfront_distribution` — `/workspace/main.tf:78`
  - `dns_records` — `/workspace/dns.tf:1`
  - `alarm_notifications` — `/workspace/monitoring.tf:1`
  - `cf_4xx_alarm` — `/workspace/monitoring.tf:17`
  - `cf_5xx_alarm` — `/workspace/monitoring.tf:40`
- [x] All module sources use private registry format `app.terraform.io/<org>/<name>/<provider>`
- [x] All module versions use pessimistic `~>` constraints
- [x] No raw `resource` blocks exist in root consumer code
- [x] Wiring table entries are implemented in code:
  - `module.content_bucket.s3_bucket_bucket_regional_domain_name -> module.cloudfront_distribution.origin.static.domain_name` — `/workspace/main.tf:97-105`
  - `module.access_logs_bucket.s3_bucket_name -> module.cloudfront_distribution.logging_config.bucket` — `/workspace/main.tf:118-122`
  - `module.acm_certificate.acm_certificate_arn -> module.cloudfront_distribution.viewer_certificate.acm_certificate_arn` — `/workspace/main.tf:124-128`
  - `module.cloudfront_distribution.cloudfront_origin_access_identity_iam_arns -> module.content_bucket.policy` via `data.aws_iam_policy_document.content_bucket_policy` — `/workspace/locals.tf:52-55`, `/workspace/data.tf:1-18`, `/workspace/main.tf:15-16`
  - `module.cloudfront_distribution.cloudfront_distribution_domain_name -> module.dns_records.records[*].alias.name` — `/workspace/dns.tf:9-18`
  - `module.cloudfront_distribution.cloudfront_distribution_hosted_zone_id -> module.dns_records.records[*].alias.zone_id` — `/workspace/dns.tf:13-17`
  - `module.cloudfront_distribution.cloudfront_distribution_id -> module.cf_4xx_alarm.dimensions.DistributionId` — `/workspace/locals.tf:47-50`, `/workspace/monitoring.tf:33-35`
  - `module.cloudfront_distribution.cloudfront_distribution_id -> module.cf_5xx_alarm.dimensions.DistributionId` — `/workspace/locals.tf:47-50`, `/workspace/monitoring.tf:56-58`
  - `module.alarm_notifications.topic_arn -> module.cf_4xx_alarm.alarm_actions` — `/workspace/monitoring.tf:35`
  - `module.alarm_notifications.topic_arn -> module.cf_5xx_alarm.alarm_actions` — `/workspace/monitoring.tf:58`
- [x] Variable declarations match the design inventory and outputs match the documented source references
- [x] Provider configuration uses remote-friendly dynamic auth comments only; no `access_key`, `secret_key`, `session_token`, `AWS_ACCESS_KEY_ID`, or `AWS_SECRET_ACCESS_KEY` references were found in `*.tf`

### Cost Controls
- Run context: `run-U1MuadgTBgZ5eA3H` in workspace `sandbox_consumer_cloudfrontcopilot-02`
- Run status: `errored`
- Trigger source: `terraform+cloud` (`Triggered via CLI`)
- Terraform version: `1.14.7`
- Run task summary: **Total tasks**: 1 | Passed: 0 | Failed: 0 | Errored: 0 | Unreachable: 1
- Task stage: `post_plan` (`status: unreachable`)
- Task result: `Apptio-Cloudability` (`status: unreachable`, enforcement: `advisory`)
- Cost reporting availability: unavailable
- Cost estimate summary: not available because the only post-plan run task was unreachable and the run has no attached cost-estimate object

### Static Analysis
- terraform fmt: PASS
- terraform validate: PASS (0 errors)
- tflint: PASS (0 issues)
- trivy: PASS (0 critical, 0 high, 3 medium, 4 low)

Notes:

- `terraform fmt -check -recursive` returned success.
- `terraform validate` returned `Success! The configuration is valid.`
- `tflint` was available and returned exit code 0 with no findings.
- `trivy config .` returned no Terraform findings in the root consumer configuration. The reported 3 medium / 4 low findings were outside the consumer root module, limited to:
  - `.devcontainer/*` Dockerfiles
  - downloaded private module source under `.terraform/modules/*`

### Quality Score
| # | Dimension | Score | Issues |
|---|-----------|-------|--------|
| 1 | Module Usage | 9.5 | All 8 modules use private registry sources with `~>` version pinning; no raw resources in root consumer code |
| 2 | Security & Compliance | 8.5 | Secure module defaults preserved, OAI enforced, encryption/logging retained, dynamic auth used; deployment blocker is environmental hosted-zone availability rather than a security weakening |
| 3 | Code Quality | 7.5 | `fmt`/`validate`/`tflint` clean and files are logically split; README and approved design still contain stale provider-version documentation |
| 4 | Variables & Outputs | 8.5 | Variable validation is materially stronger than before, outputs match design, and shortened bucket naming removes the earlier S3 length-risk path; hosted-zone existence is still only provable at plan time |
| 5 | Wiring & Integration | 7.5 | All 10 documented wiring edges are implemented correctly, but unattended deployment still fails when `hosted_zone_name` does not resolve to a public zone in the target account |
| 6 | Constitution Alignment | 7.0 | Consumer code now satisfies private-registry, `~>` provider, dynamic-auth, and no-raw-resource rules; design/README drift and failed sandbox plan still leave the validation gate incomplete |

Overall: 8.4/10.0 — Excellent
Production Readiness: Not Ready

### Sandbox Deployment
- Workspace: `sandbox_consumer_cloudfrontcopilot-02`
- Run URL: https://app.terraform.io/app/hashi-demos-apj/workspaces/sandbox_consumer_cloudfrontcopilot-02/runs/run-U1MuadgTBgZ5eA3H
- Plan: FAIL
- Apply: SKIPPED
- Resources: N/A/N/A/N/A
- Cost Estimate: N/A

Deployment notes:

- The remote run errored during planning before a plan artifact or cost estimate was produced.
- The active blocker remains Route53 hosted-zone discovery for the unattended input set.
- `data.aws_route53_zone.this` in `/workspace/main.tf:1-4` looks up the public zone named by `/workspace/terraform.auto.tfvars:3`.
- With the current unattended values in `/workspace/terraform.auto.tfvars:2-4`, the workspace credentials still do not see a matching public hosted zone for `hashidemos.io`.

### Issues Requiring Manual Fix
- `/workspace/main.tf:1-4`, `/workspace/terraform.auto.tfvars:2-4` — The sandbox run still fails because `hosted_zone_name = "hashidemos.io"` assumes a public Route53 hosted zone exists and is visible to the workspace credentials. Fix by supplying a hosted zone that actually exists in the target account, or by creating/sharing the expected public zone before rerunning.
- `/workspace/specs/001-cloudfront-static-content/consumer-design.md:6` — The approved design still documents `Provider: aws ~> 5.0`, while `/workspace/versions.tf:5-8` now constrains `hashicorp/aws` to `~> 6.0`. Update the design so the approved source of truth matches the implementation.
- `/workspace/README.md:107-113` — Generated requirements/provider documentation still shows `>= 5.0` for AWS. Regenerate or update the README so operator documentation matches `versions.tf`.
